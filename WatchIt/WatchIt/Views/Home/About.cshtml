@{
    ViewBag.Title = "About";
}
<h2>@ViewBag.Title.</h2>

@{
    var branchController = new WatchIt.Controllers.BranchesController();
    var allBranches = (List<WatchIt.Models.Branch>)branchController.GetAllBranches();
    var firstBranch = (WatchIt.Models.Branch)branchController.GetFirstBranch();
    var mainBranchName = firstBranch.BranchName;
    var moreBranches = (List<WatchIt.Models.Branch>)branchController.GetOtherBranches(firstBranch.BranchID);

}
<style>
    #map {
        width: 100%;
        height: 400px;
        background-color: grey;
    }
</style>
<script>
    // Initialize and add the map
    function initMap() {
        var map = new google.maps.Map(document.getElementById('map'), {
            zoom: 4,
            center: { lat: @firstBranch.BranchLat, lng:  @firstBranch.BranchLng}
        });

        addMarker({ lat:  @firstBranch.BranchLat, lng: @firstBranch.BranchLng}, map)
    }

    function putMarkersOnMap() {
        for (int index = 0; index < @allBranches.Count(); index++) {
           
            //addMarker(location()
        }
        for (let i in @allBranches) {
            
        }
        
    }

    //var marker = new google.maps.Marker({
    //    position: uluru,
    //    map: map
    //});
            function addMarker(location, map) {
                var marker = new google.maps.Marker({
                position: location,
                map: map
                });
        
        //var uluru = { lat: -25.344, lng: 131.036 };
    }
</script>
<div id="map"></div>
<script async defer
        src="https://maps.googleapis.com/maps/api/js?key=AIzaSyAgAZnyesXFkkS0GQNhadDGPLKPdQD8m_E&callback=initMap">
</script>

